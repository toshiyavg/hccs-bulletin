<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.Caps">

    <application
        android:allowBackup="true"
        android:icon="@drawable/bulletin_board"
        android:label="HCCS Corner"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProfPop"
            android:theme="@style/ViewProf"></activity>
        <activity android:name=".Chat" android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <activity
            android:name=".profpic_view"
            android:excludeFromRecents="true"
            android:theme="@style/ViewProf" />
        <activity android:name=".AddForm" />
        <activity android:name="com.example.myapplication.Add_Form" />
        <activity android:name=".homeForm" />
        <activity android:name=".loginForm" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PopupPost" />
    </application>

</manifest>